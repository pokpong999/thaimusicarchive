thma_full_2026-08-25.zip — อัปทับได้เลย ครบในตัว
════════════════════════════════════════════════
ชุดนี้ครบในตัว ไม่ต้องอัปชุดไหนก่อน ไม่ต้องอัปชุดไหนตาม
รวมงานที่ค้างทั้งหมด: กระดานโน้ต v3 · แก้ความปลอดภัย · แก้ XSS
                      · สมัครสมาชิก · ความคิดเห็น · แจ้งเตือน · ย่อรูป · ภาพแชร์

┏━━ ผลทดสอบก่อนส่ง ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
┃ ผมเอาไฟล์ชุดนี้ไปวางทับเว็บจริงในเครื่องผม แล้วสั่ง next build
┃ แบบเดียวกับที่ Vercel ทำ
┃    ✅ build ผ่านทั้ง 34 หน้า ไม่มี error
┃    ✅ import ภายในโปรเจกต์ 138 จุด ชี้ไปไฟล์ที่มีจริงครบ
┃    ✅ ไฟล์ทั้ง 25 ไฟล์ผ่านการตรวจ syntax
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┏━━ ทำไมชุดก่อนหน้าอัปทับเดี่ยว ๆ แล้วอาจพัง ━━━━━━━━━━━━━━━━━━━
┃ app/admin/page.js ในชุดนั้น import lib/notation-core และ
┃ components/NotationInput ซึ่งมาจาก thma_notation_v3.zip
┃ ถ้ายังไม่ได้อัป v3 ไฟล์สองตัวนั้นไม่มีอยู่ → Vercel build ล้ม
┃ ชุดนี้ใส่ไฟล์ที่ขาดมาให้ครบแล้ว จึงอัปทับได้เลยไม่ว่าสถานะเดิมเป็นอย่างไร
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┏━━ ลำดับติดตั้ง ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
┃ Supabase → SQL Editor  รันทีละไฟล์
┃   1. sql/00_DIAG_signup.sql    (อ่านอย่างเดียว — ส่งผลกลับมาให้ผมดู)
┃   2. sql/05b_FIX_signup.sql    แก้สมัครสมาชิกไม่ได้
┃   3. sql/06_comments.sql       แก้ความคิดเห็นไม่แสดง
┃   4. sql/07_notifications.sql  ระบบแจ้งเตือน
┃ GitHub
┃   5. ลาก app + components + lib ทับ → Commit  (อย่าลาก sql ขึ้น GitHub)
┃   6. รอ Vercel build → Ctrl+F5
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

โฟลเดอร์ sql/ชุดเก่า-เผื่อยังไม่ได้รัน/
  01 โครงสร้างกระดานโน้ต · 02 สิทธิ์แก้โน้ต · 03 ฉิ่ง-อัตราชั้น
  04 ฟังก์ชันแอดมิน · 05 แก้เปลี่ยน role ไม่ได้
  ทั้งหมดรันซ้ำได้ ไม่ทำลายข้อมูลเดิม
  ผมตรวจฐานจริงแล้วพบว่า 01 กับ 03 รันไปแล้วแน่นอน (song_melody มีคอลัมน์
  section / line_no / krasuan / level / ching ครบ) ส่วน 02 04 05 ยังไม่แน่ใจ
  ถ้าไม่แน่ใจ รันซ้ำทั้งหมดได้เลย ปลอดภัย

── ไฟล์ในชุดนี้ (25 ไฟล์) ───────────────────────────────────────
lib/
  notation-core.js notation-engine.js       กระดานโน้ต v3
  sampler.js nathab.js                      เสียงฆ้อง · หน้าทับ-ฉิ่ง
  htmlesc.js i18n.js                        กัน XSS ในแผนที่
  imgresize.js                              ย่อรูปก่อนอัปโหลด  ★ใหม่
components/
  NotationInput.js NotationPlayer.js StaffNotation.js
  CommentSection.js                         แก้ความคิดเห็นหาย  ★
  NotificationBell.js                       แจ้งเตือน  ★
  ShareBar.js                               ภาพแชร์สดเสมอ  ★
  Editable.js                               ย่อรูปหน้าเว็บ  ★
app/
  songs/new/page.js  songs/[id]/edit/page.js          เพิ่ม/แก้โน้ตด้วยกระดาน
  songs/[id]/page.js  songs/[id]/SongDetailClient.js  ★ อ่านคอมเมนต์ได้ทุกคน
  admin/page.js                              ★ รายการความคิดเห็นกลับมาแสดง
  login/page.js                              ★ สมัครสมาชิก
  profile/page.js  archive/new/page.js       ★ ย่อรูป
  archive/page.js  archive/[id]/ArchiveDetailClient.js   กัน XSS
  archive/[id]/opengraph-image.js            ★ ภาพแชร์ติดรูป

── ทดสอบหลังติดตั้ง ────────────────────────────────────────────
  □ สมัครสมาชิกใหม่ด้วยอีเมลที่ไม่เคยใช้ → เข้าเว็บได้ทันที
  □ /archive/282 → เห็นความคิดเห็น 2 รายการที่หายไป กลับมาแล้ว
  □ ล็อกเอาต์ เปิดหน้าเพลง → อ่านความคิดเห็นได้ แต่ไม่มีช่องพิมพ์
  □ สมาชิกส่งบันทึกจดหมายเหตุ → กระดิ่ง 🔔 ของแอดมินขึ้นเลข
  □ กดอนุมัติใน /admin → กระดิ่งของสมาชิกคนนั้นขึ้นเลข
  □ แชร์บันทึกที่มีรูปลง Facebook → การ์ดมีรูป
  □ /songs/new → พิมพ์ a s d f เห็นโน้ตขึ้นและมีเสียง
