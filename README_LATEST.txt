thma_player_LATEST.zip — เครื่องเล่นโน้ต THMA ชุดล่าสุด (2026-08-24)

ไฟล์ในชุดนี้ (อัปทับได้เลย ลาก components + lib ลง GitHub → Commit → Ctrl+F5)
  components/NotationPlayer.js  ← แก้
  components/StaffNotation.js   ← แก้ (โน้ตสากลล้นหน้า)
  lib/sampler.js                ← แก้
  lib/nathab.js                 ← เท่ากับบน GitHub

สิ่งที่เปลี่ยนจากไฟล์บน GitHub (ตรวจแล้วว่า GitHub มีรุ่น ensemble + dropdown สะบัดอยู่แล้ว ไม่ได้ถูกทับ)
  1. sampler.js — ตัดความเงียบต้นไฟล์ mp3 ของฆ้องแต่ละลูกอัตโนมัติ (detectOnset)
     mp3 แต่ละไฟล์มีช่วงเงียบต้นไฟล์ไม่เท่ากัน → สะบัด 3 ตัวที่นัดห่าง 60 ms ดังห่างไม่เท่ากัน
     นี่คือสาเหตุที่น่าจะเป็นไปได้มากที่สุดของ "สะบัดเพี้ยนทั้งที่โค้ดถูก"
  2. NotationPlayer.js — สะบัดที่อยู่ตำแหน่งแรกของวรรค ดึงตัวนำจากตำแหน่งสุดท้ายของวรรคก่อนได้แล้ว
     (เดิมได้แค่ 2 ตัว และตัวนำดังอยู่ที่จังหวะเดิม) และสะบัดตรงห้องที่กดเล่น (seek) ก็ได้ตัวนำครบ
  3. NotationPlayer.js — dropdown ช่องไฟสะบัดแสดงค่าที่เลือกถูกต้อง (เดิม value "0.060" ไม่ตรง state 0.06
     ทำให้ช่องแสดงว่าง/ผิดตัว แม้ค่าจริงยังเป็น 60 ms)
  4. NotationPlayer.js — น้ำหนักสะบัด ตัวนำเบากว่าตัวลง (0.6 → 0.8 → 1.0) และเผื่อเวลาเปิดเพลง 0.25 วิ
     กันตัวนำของสะบัดตัวแรกของเพลงถูกเบียดในเพลงยาว
  5. StaffNotation.js — โน้ตสากลไม่ล้นกล่องอีก: คำนวณความกว้างห้องจากจำนวนห้องจริงของแต่ละบรรทัด
     (เดิมคิดจาก 8 ห้องคงที่ แต่แบบสากลแบ่งได้ 9-11 ห้อง/บรรทัด จึงล้น) · ห้องยก/ห้องเศษแคบกว่าห้องเต็ม
     · ห้องยกท้ายวรรครวมกับห้องต้นวรรคถัดไปในบรรทัดเดียวกัน · จอแคบ (มือถือ) ลดจำนวนวรรคต่อบรรทัดให้พอดี
